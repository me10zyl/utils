package com.yilnz.bluesteel.processor;

/*
import com.sun.tools.javac.tree.JCTree;
import com.sun.tools.javac.tree.JCTree.JCAnnotation;
import com.sun.tools.javac.util.List;
import lombok.core.AnnotationValues;
import lombok.javac.JavacAnnotationHandler;
import lombok.javac.JavacNode;
import lombok.javac.JavacTreeMaker;
import lombok.javac.handlers.JavacHandlerUtil;
import org.mangosdk.spi.ProviderFor;

@ProviderFor(JavacAnnotationHandler.class)
public class GenerateInterfaceHandling extends JavacAnnotationHandler<GenerateInterface> {

    @Override
    public void handle(AnnotationValues<GenerateInterface> annotation, JCAnnotation ast, JavacNode annotationNode) {
        JavacNode typeNode = annotationNode.up();
        JCTree.JCClassDecl jcClassDecl = (JCTree.JCClassDecl) typeNode.get();
        List<JCTree.JCExpression> implementing = jcClassDecl.implementing;
        System.out.println(implementing);
    }
}

*/
